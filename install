#!/bin/bash

GITCONFIG=.gitconfig
VIMCONFIG=.vimrc
EMACSCONFIG=.emacs
VIMDIR=.vim
ERROR=false

if [ -z "$HOME" ]; then
  echo "ERROR: variable HOME not set."
  exit 1
fi

if [ -e "$HOME/$GITCONFIG" ]; then
  echo "ERROR: $GITCONFIG already exists, please remove or backup old."
  ERROR=true
fi

if [ -e "$HOME/$EMACSCONFIG" ]; then
  echo "ERROR: $EMACSCONFIG already exists, please remove or backup old."
  ERROR=true
fi

if [ -e "$HOME/$VIMCONFIG" ]; then
  echo "ERROR: $VIMCONFIG already exists, please remove or backup old."
  ERROR=true
fi

if [ -e "$HOME/$VIMDIR" ]; then
  echo "ERROR: $VIMDIR already exists, please remove or backup old."
  ERROR=true
fi

[ "$ERROR" = "true" ] && exit 1


# Link config files
ln -s $PWD/$GITCONFIG $HOME/$GITCONFIG
ln -s $PWD/$EMACSCONFIG $HOME/$EMACSCONFIG
ln -s $PWD/$VIMCONFIG $HOME/$VIMCONFIG
ln -s $PWD/$VIMDIR $HOME/$VIMDIR

echo "Created symbolic links for config files in $HOME"
